# Wrkr Contracts

Stable OSS contracts include:

- Primitive behavior and lifecycle semantics
- Checkpoint protocol schema and reason codes
- Jobpack manifest and verification behavior
- Acceptance result and stable exit code surface
- Versioned schema artifacts under `schemas/v1/*`

Reference: `docs/contracts/primitive_contract.md`.
